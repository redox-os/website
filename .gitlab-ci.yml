before_script:
  - apt-get update -qq && apt-get install -y -qq hugo gcc pkg-config libc6-dev libssl-dev
  - cargo install lychee

hugo:
  script: "./hugo.sh"
  artifacts:
    paths:
    - build/public

lychee:
  script: "lychee content"
  artifacts:
    paths:
    - build/public
